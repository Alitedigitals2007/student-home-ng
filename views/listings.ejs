<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Explore Rooms | StudentHome</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;800&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Plus Jakarta Sans', sans-serif; }
        .no-scrollbar::-webkit-scrollbar { display: none; }
    </style>
</head>
<body class="bg-white text-black antialiased">

    <nav class="flex justify-between items-center px-6 md:px-10 py-8 border-b border-gray-50 bg-white sticky top-0 z-50">
        <a href="/" class="text-2xl font-black tracking-tighter uppercase">Student<span class="text-gray-300">Home</span></a>
        <div class="flex items-center gap-6">
            <a href="/requests" class="text-[10px] font-black uppercase tracking-widest hidden md:block text-gray-400 hover:text-black transition">Request Board</a>
            <a href="/dashboard" class="text-[10px] font-black uppercase tracking-widest border-b-2 border-black pb-1">My Dashboard</a>
        </div>
    </nav>

    <main class="max-w-7xl mx-auto px-6 md:px-10 py-12 md:py-20">
        <header class="mb-12">
            <h2 class="text-5xl md:text-7xl font-black tracking-tighter uppercase leading-none">
                Find your <br><span class="text-gray-200">Perfect Space</span>
            </h2>
        </header>

        <form action="/listings" method="GET" class="mb-20 bg-white shadow-[0_40px_80px_-15px_rgba(0,0,0,0.08)] rounded-[2.5rem] p-6 border border-gray-50 flex flex-wrap gap-4 items-end">
            <div class="flex-1 min-w-[250px]">
                <label class="text-[10px] font-black uppercase tracking-widest text-gray-300 ml-4">Campus Location</label>
                <input type="text" name="location" placeholder="Search by area or campus..." class="w-full bg-gray-50 rounded-2xl px-6 py-4 outline-none font-bold mt-1 focus:ring-2 ring-black transition-all">
            </div>
            
            <div class="w-full md:w-40">
                <label class="text-[10px] font-black uppercase tracking-widest text-gray-300 ml-4">Min ₦</label>
                <input type="number" name="min_price" placeholder="0" class="w-full bg-gray-50 rounded-2xl px-6 py-4 outline-none font-bold mt-1">
            </div>

            <div class="w-full md:w-40">
                <label class="text-[10px] font-black uppercase tracking-widest text-gray-300 ml-4">Max ₦</label>
                <input type="number" name="max_price" placeholder="Any" class="w-full bg-gray-50 rounded-2xl px-6 py-4 outline-none font-bold mt-1">
            </div>

            <button type="submit" class="w-full md:w-auto bg-black text-white px-10 py-5 rounded-2xl font-bold uppercase text-xs tracking-widest hover:bg-gray-800 transition shadow-lg active:scale-95 transform">
                Apply Filters
            </button>
        </form>

        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-x-10 gap-y-16">
            <% if (rooms && rooms.length > 0) { %>
                <% rooms.forEach(room => { %>
                    <div class="group relative">
                        <a href="/room/<%= room.id %>" class="block">
                            <div class="aspect-[4/5] bg-gray-100 rounded-[2.5rem] mb-6 overflow-hidden relative border border-gray-50">
                                <img src="<%= room.image_url || 'https://images.unsplash.com/photo-1522708323590-d24dbb6b0267?q=80&w=800' %>" 
                                     class="w-full h-full object-cover group-hover:scale-105 transition duration-700 ease-out"
                                     alt="<%= room.title %>">
                                
                                <div class="absolute top-6 left-6">
                                    <span class="bg-white/90 backdrop-blur-md text-black px-4 py-2 rounded-full text-[8px] font-black uppercase tracking-[0.15em] shadow-sm">
                                        <%= room.status %>
                                    </span>
                                </div>
                            </div>

                            <div class="flex justify-between items-start px-2">
                                <div class="max-w-[65%]">
                                    <h3 class="font-bold text-xl tracking-tight leading-tight mb-1 truncate group-hover:underline"><%= room.title %></h3>
                                    <div class="flex items-center gap-1 text-gray-400">
                                        <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"></path><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"></path></svg>
                                        <p class="text-[11px] font-bold uppercase tracking-widest"><%= room.location %></p>
                                    </div>
                                </div>
                                <div class="text-right">
                                    <p class="font-black text-2xl tracking-tighter">₦<%= Number(room.price).toLocaleString() %></p>
                                    <p class="text-[8px] font-black uppercase text-gray-300 tracking-[0.2em]">Per Year</p>
                                </div>
                            </div>
                        </a>

                        <a href="/room/<%= room.id %>" class="block w-full mt-6">
                            <button class="w-full py-5 bg-gray-50 group-hover:bg-black group-hover:text-white rounded-2xl text-[10px] font-black uppercase tracking-[0.2em] transition-all duration-300">
                                View Details
                            </button>
                        </a>
                    </div>
                <% }) %>
            <% } else { %>
                <div class="col-span-full py-32 text-center bg-gray-50 rounded-[4rem] border-2 border-dashed border-gray-100">
                    <p class="text-gray-400 font-black uppercase tracking-[0.3em] text-xs mb-4">No results found</p>
                    <a href="/listings" class="inline-block bg-white border border-gray-100 px-8 py-4 rounded-2xl text-[10px] font-black uppercase tracking-widest hover:bg-black hover:text-white transition">
                        Clear Filters
                    </a>
                </div>
            <% } %>
        </div>
    </main>

    <footer class="py-20 text-center border-t border-gray-50 mt-20">
        <p class="text-[10px] font-black uppercase tracking-[0.4em] text-gray-300">&copy; 2026 StudentHome Premium</p>
    </footer>

</body>
</html>