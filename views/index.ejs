<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>StudentHome | Premium Housing Network</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;800&display=swap" rel="stylesheet">
    <script src="https://unpkg.com/typewriter-effect@latest/dist/core.js"></script>
    <style>
        body { font-family: 'Plus Jakarta Sans', sans-serif; scroll-behavior: smooth; }
        .typewriter-cursor { color: #e5e7eb; font-weight: 200; }
        .fade-in { animation: fadeIn 2s ease-in forwards; opacity: 0; }
        @keyframes fadeIn { from { opacity: 0; transform: translateY(20px); } to { opacity: 1; transform: translateY(0); } }
        .glass { background: rgba(255, 255, 255, 0.8); backdrop-filter: blur(12px); }
        .mobile-menu { transition: transform 0.3s ease-in-out; }
    </style>
</head>
<body class="bg-white text-black antialiased">

    <nav class="flex justify-between items-center px-6 md:px-10 py-6 md:py-8 border-b border-gray-50 glass fixed w-full top-0 z-[100]">
        <div class="text-xl md:text-2xl font-black tracking-tighter uppercase">Student<span class="text-gray-300">Home</span></div>
        
        <div class="hidden md:flex items-center gap-8">
            <a href="/listings" class="text-[10px] font-bold uppercase tracking-[0.2em] hover:text-gray-400 transition">Rooms</a>
            <a href="/roommates" class="text-[10px] font-bold uppercase tracking-[0.2em] hover:text-gray-400 transition">Roommates</a>
            <a href="/requests" class="text-[10px] font-bold uppercase tracking-[0.2em] hover:text-gray-400 transition">Request Board</a>
            <a href="/login" class="bg-black text-white px-8 py-3 rounded-full text-[10px] font-bold uppercase tracking-widest shadow-lg hover:bg-gray-800 transition">Member Access</a>
        </div>

        <button id="menuBtn" class="md:hidden text-black focus:outline-none">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16m-7 6h7" />
            </svg>
        </button>
    </nav>

    <div id="mobileMenu" class="fixed inset-0 bg-white z-[90] flex flex-col items-center justify-center gap-8 translate-x-full mobile-menu md:hidden">
        <a href="/listings" class="text-2xl font-black uppercase tracking-tighter">Rooms</a>
        <a href="/roommates" class="text-2xl font-black uppercase tracking-tighter">Roommates</a>
        <a href="/requests" class="text-2xl font-black uppercase tracking-tighter">Request Board</a>
        <a href="/login" class="bg-black text-white px-10 py-4 rounded-full text-xs font-bold uppercase tracking-widest">Sign In</a>
    </div>

    <section class="min-h-screen flex flex-col justify-center items-center px-6 text-center bg-[#fafafa] relative overflow-hidden pt-20">
        <div class="absolute top-20 left-10 w-48 md:w-64 h-48 md:h-64 bg-gray-100 rounded-full mix-blend-multiply filter blur-3xl opacity-70 animate-pulse"></div>
        
        <div class="z-10 w-full">
            <h1 class="text-5xl md:text-[110px] font-black tracking-tighter leading-[0.85] mb-8 md:mb-12">
                <span id="typewriter-main"></span><br>
                <span id="typewriter-sub" class="text-gray-200"></span>
            </h1>

            <div class="flex flex-col md:flex-row justify-center gap-4 md:gap-6 fade-in" style="animation-delay: 1.5s;">
                <a href="/listings" class="bg-black text-white px-10 md:px-12 py-5 md:py-6 rounded-[2.5rem] font-bold text-sm shadow-2xl hover:scale-105 transition transform active:scale-95 group">
                    Find a House <span class="inline-block transition-transform group-hover:translate-x-2 ml-2">→</span>
                </a>
                <a href="/requests" class="bg-white text-black border-2 border-black px-10 md:px-12 py-5 md:py-6 rounded-[2.5rem] font-bold text-sm hover:bg-black hover:text-white transition transform hover:scale-105 active:scale-95">
                    I Need a Room
                </a>
            </div>
        </div>
    </section>

    <section class="py-24 bg-white px-6 md:px-10">
        <div class="max-w-7xl mx-auto flex flex-col md:flex-row items-center gap-16">
            <div class="flex-1">
                <h2 class="text-5xl md:text-7xl font-black tracking-tighter uppercase leading-none mb-6">Can't find<br>the right<br><span class="text-gray-200">spot?</span></h2>
                <p class="text-gray-400 font-medium mb-8 max-w-sm text-lg">Post your budget and location on our request board. Let the rooms find you.</p>
                <a href="/requests" class="text-sm font-bold uppercase tracking-widest border-b-4 border-black pb-2 hover:text-gray-400 hover:border-gray-200 transition">Go to Request Board</a>
            </div>
            <div class="flex-1 w-full grid grid-cols-1 gap-4">
                <div class="bg-[#fafafa] p-8 rounded-[2rem] border border-gray-50 transform rotate-1">
                    <p class="text-[10px] font-black uppercase text-gray-400 mb-2">Recent Request</p>
                    <p class="font-bold text-xl leading-tight mb-4">"Looking for a self-contain near Unilag Gate. Budget is ₦450k max."</p>
                    <div class="flex items-center gap-3">
                        <div class="w-8 h-8 bg-gray-200 rounded-full"></div>
                        <span class="text-xs font-bold uppercase">Mustapha K.</span>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <section class="py-24 md:py-32 bg-[#fafafa] px-6 md:px-10">
        <div class="max-w-7xl mx-auto text-center md:text-left">
            <div class="mb-20">
                <h2 class="text-5xl font-black tracking-tighter uppercase mb-4">The Student<span class="text-gray-300">Standard</span></h2>
                <p class="text-gray-400 max-w-md mx-auto md:mx-0 font-medium text-lg">Three ways we're changing the game.</p>
            </div>

            <div class="grid grid-cols-1 md:grid-cols-3 gap-8 md:gap-12">
                <div class="p-10 bg-white rounded-[3rem] border border-gray-50">
                    <h3 class="font-black text-2xl mb-4 tracking-tight uppercase">Roommates</h3>
                    <p class="text-gray-400 text-sm leading-relaxed">Don't live with a stranger. Match based on habits, vibes, and budgets.</p>
                </div>
                <div class="p-10 bg-white rounded-[3rem] border border-gray-50">
                    <h3 class="font-black text-2xl mb-4 tracking-tight uppercase">Requests</h3>
                    <p class="text-gray-400 text-sm leading-relaxed">Broadcast what you need. Landlords and students will reply to you directly.</p>
                </div>
                <div class="p-10 bg-white rounded-[3rem] border border-gray-50">
                    <h3 class="font-black text-2xl mb-4 tracking-tight uppercase">Listings</h3>
                    <p class="text-gray-400 text-sm leading-relaxed">Verified photos and direct contact. No agent run-around.</p>
                </div>
            </div>
        </div>
    </section>

    <section class="py-20 md:py-32 px-6 md:px-10">
        <div class="max-w-7xl mx-auto bg-black rounded-[3rem] md:rounded-[4rem] p-12 md:p-32 text-center text-white relative overflow-hidden">
            <div class="relative z-10">
                <h2 class="text-4xl md:text-7xl font-black tracking-tighter leading-none mb-10">Secure your<br class="hidden md:block"> premium spot.</h2>
                <a href="/register" class="bg-white text-black px-12 py-5 md:py-6 rounded-full font-bold uppercase text-[10px] md:text-xs tracking-widest hover:bg-gray-200 transition">Get Started Now</a>
            </div>
        </div>
    </section>

    <footer class="py-20 border-t border-gray-50 text-center px-6">
        <div class="text-xl font-black tracking-tighter uppercase mb-6">Student<span class="text-gray-300">Home</span></div>
        <p class="text-[10px] font-bold uppercase tracking-[0.3em] text-gray-300">Premium Student Housing Network © 2026</p>
    </footer>

    <script>
        // Mobile Menu Logic
        const menuBtn = document.getElementById('menuBtn');
        const mobileMenu = document.getElementById('mobileMenu');
        menuBtn.addEventListener('click', () => {
            mobileMenu.classList.toggle('translate-x-full');
        });

        // Typewriter Animation
        const mainText = document.getElementById('typewriter-main');
        const subText = document.getElementById('typewriter-sub');

        new Typewriter(mainText, {
            strings: ['Your Student Life.', 'Your New Space.'],
            autoStart: true,
            loop: true,
            delay: 130, 
            pauseFor: 3000
        });

        setTimeout(() => {
            new Typewriter(subText, {
                strings: ['Better Together.', 'Live Premium.'],
                autoStart: true,
                loop: true,
                delay: 160,
                pauseFor: 3500
            });
        }, 1800);
    </script>
</body>
</html>